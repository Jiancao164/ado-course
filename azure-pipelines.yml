resources:
  repositories:
    - repository: templates
      type: git
      name: templates
      ref: refs/heads/main

extends:
  template: build-and-test.yml@templates
  parameters:
    buildConfiguration: Debug
    runTests: true
